---

##
# Handler and daemon reload behaviour, exercised on the LMDB instance of the
# scenario. The other backend instances are left untouched.
#
# Every step re-runs the role with `pdns_flush_handlers: true`, so the notified
# handlers run before the assertions instead of at the end of the play, and the
# last step restores the state the converge left, because the verifier runs
# after this playbook.
##

- name: Handler and daemon reload behaviour
  hosts: pdns
  vars_files:
    - "../resources/vars/pdns-repo-{{ lookup('env', 'MOLECULE_SCENARIO_NAME') }}.yml"
    - ../resources/vars/pdns-common.yml
    - ../resources/vars/pdns-instance-vars.yml
    - ../resources/vars/pdns-backend-lmdb.yml
  vars:
    _unit: "{{ pdns_instance.service_name }}"
    _dropin_dir: "/etc/systemd/system/{{ pdns_instance.service_name }}.service.d"
    _rotated_api_key: rotated-molecule-api-key
    # Built from the same three layers as pdns_config in
    # ../resources/vars/pdns-instance-vars.yml rather than from pdns_config
    # itself. Deriving it from pdns_config and then passing the result back in as
    # the include_role variable of that same name is a self-reference: inside the
    # role pdns_config would resolve to this expression, which resolves to
    # pdns_config, and ansible aborts the play with "recursive loop detected in
    # template string".
    _rotated_config: >-
      {{ pdns_config_common
         | combine(pdns_instance.config_overrides | default({}), recursive=true)
         | combine(pdns_config_overrides | default({}), recursive=true)
         | combine({'api-key': _rotated_api_key}) }}
    _probe_zone: api-key-rotation.test.
    # Read from the merged configuration, not from the instance overrides, so it
    # still resolves for a backend profile that does not override the port.
    _api_url: >-
      http://127.0.0.1:{{ pdns_config['webserver-port'] }}/api/v1/servers/localhost
  tasks:

    - name: Skip the whole playbook on hosts without systemd
      ansible.builtin.meta: end_host
      when: ansible_facts.service_mgr != 'systemd'

    # The drop-in the converge wrote, as the baseline every step builds on.
    - name: Record the configured overrides
      ansible.builtin.set_fact:
        _base_service_overrides: "{{ pdns_service_overrides }}"

    ##
    # A run that writes a drop-in has to tell systemd about it, even when no
    # handler restarts anything: the handlers flush at the end of the play, so
    # until then systemd would hand out the unit it loaded before the run, and a
    # service started in between - by the service task of a later role, or by an
    # operator - would come up with the settings of the stale unit.
    #
    # `NeedDaemonReload` is what systemd answers with while its copy of the unit
    # is older than the files on disk. Handlers are disabled here, so only the
    # reload of the service task itself can clear it. The running process keeps
    # its old limits until something restarts it, which is what the reload in
    # the restart handler is for.
    ##
    - name: Change the drop-in with the handlers disabled
      ansible.builtin.include_role:
        name: powerdns.pdns
      vars:
        pdns_disable_handlers: true
        pdns_flush_handlers: true
        pdns_service_overrides: "{{ _base_service_overrides | combine({'LimitNOFILE': 12345}) }}"

    - name: Read the unit after the drop-in changed
      ansible.builtin.systemd_service:
        name: "{{ _unit }}"
      register: _unit_after_start
      changed_when: false

    - name: Assert that systemd was told about the drop-in of this run
      ansible.builtin.assert:
        that:
          - _unit_after_start.status.NeedDaemonReload == 'no'
          - _unit_after_start.status.LimitNOFILE | int == 12345
          - _unit_after_start.status.ActiveState == 'active'
        fail_msg: >-
          systemd still holds the unit it loaded before this run:
          NeedDaemonReload={{ _unit_after_start.status.NeedDaemonReload }},
          LimitNOFILE={{ _unit_after_start.status.LimitNOFILE }},
          ActiveState={{ _unit_after_start.status.ActiveState }}

    ##
    # A drop-in changed while the service is deliberately stopped still has to
    # reach systemd: the restart handler omits the restart and keeps the reload.
    ##
    - name: Change the drop-in while the service is stopped
      ansible.builtin.include_role:
        name: powerdns.pdns
      vars:
        pdns_flush_handlers: true
        pdns_service_state: stopped
        pdns_service_overrides: "{{ _base_service_overrides | combine({'LimitNOFILE': 4242}) }}"

    - name: Read the unit of the stopped service
      ansible.builtin.systemd_service:
        name: "{{ _unit }}"
      register: _unit_stopped
      changed_when: false

    - name: Assert that the stopped unit was reloaded but not started
      ansible.builtin.assert:
        that:
          - _unit_stopped.status.LimitNOFILE | int == 4242
          - _unit_stopped.status.ActiveState == 'inactive'
        fail_msg: >-
          LimitNOFILE={{ _unit_stopped.status.LimitNOFILE }},
          ActiveState={{ _unit_stopped.status.ActiveState }}

    ##
    # Emptying the override dict removes the drop-in of the role alone: a
    # drop-in the operator added themselves stays, and the service restarts on
    # the unit without the removed settings.
    ##
    - name: Add a drop-in of the operator next to the one of the role
      ansible.builtin.copy:
        content: |
          [Service]
          LimitMSGQUEUE=1234567
        dest: "{{ _dropin_dir }}/10-operator.conf"
        owner: root
        group: root
        mode: '0644'

    - name: Read the unit before the overrides are emptied
      ansible.builtin.systemd_service:
        name: "{{ _unit }}"
      register: _unit_before_removal
      changed_when: false

    - name: Empty the override dict
      ansible.builtin.include_role:
        name: powerdns.pdns
      vars:
        pdns_flush_handlers: true
        pdns_service_overrides: {}

    - name: Stat the drop-ins after the overrides were emptied
      ansible.builtin.stat:
        path: "{{ _dropin_dir }}/{{ item }}"
      loop:
        - override.conf
        - 10-operator.conf
      register: _dropins_after_removal

    - name: Read the unit after the overrides were emptied
      ansible.builtin.systemd_service:
        name: "{{ _unit }}"
      register: _unit_after_removal
      changed_when: false

    - name: Assert that only the drop-in of the role was removed
      ansible.builtin.assert:
        that:
          - not _stats['override.conf']
          - _stats['10-operator.conf']
        fail_msg: "drop-ins found on the host: {{ _stats }}"
      vars:
        _stats: >-
          {{ dict(_dropins_after_removal.results
                  | map(attribute='item')
                  | zip(_dropins_after_removal.results | map(attribute='stat.exists'))) }}

    - name: Assert that the removal reloaded the unit and restarted the service
      ansible.builtin.assert:
        that:
          - _unit_after_removal.status.LimitNOFILE | int != 4242
          - _unit_after_removal.status.LimitMSGQUEUE | int == 1234567
          - >-
            _unit_after_removal.status.ActiveEnterTimestampMonotonic | int
            > _unit_before_removal.status.ActiveEnterTimestampMonotonic | int
        fail_msg: >-
          before={{ _unit_before_removal.status.ActiveEnterTimestampMonotonic }},
          after={{ _unit_after_removal.status.ActiveEnterTimestampMonotonic }},
          LimitNOFILE={{ _unit_after_removal.status.LimitNOFILE }}

    ##
    # Provisioning in the same run that changes the API key.
    #
    # The key lives in the configuration file, so changing it rewrites the file
    # and notifies a restart, while the instance carries on serving the old key
    # until that restart happens. Provisioning authenticates with the key that is
    # configured now, so it can only succeed once the restart has been done -
    # which is why the role flushes the handlers before provisioning starts
    # rather than at the end of the play. Without that flush every request here
    # is answered 401 and the play fails.
    #
    # Both requests below are made with the rotated key alone. Reaching the API
    # with it at all is the assertion: the old key would still be in the
    # configuration, and still in use by the running instance, had either half of
    # this failed.
    ##
    - name: Change the API key and provision in the same run
      ansible.builtin.include_role:
        name: powerdns.pdns
      vars:
        pdns_flush_handlers: true
        pdns_config: "{{ _rotated_config }}"
        pdns_provision: true
        pdns_provision_zones:
          - name: "{{ _probe_zone }}"
            kind: Native
            nameservers:
              - "ns1.{{ _probe_zone }}"
        pdns_provision_zone_metadata:
          - zone: "{{ _probe_zone }}"
            kind: ALLOW-AXFR-FROM
            metadata:
              - 127.0.0.0/8

    - name: Read the provisioned zone with the rotated API key
      ansible.builtin.uri:
        headers:
          X-Api-Key: "{{ _rotated_api_key }}"
        url: "{{ _api_url }}/zones/{{ _probe_zone }}"
        method: GET
        status_code:
          - 200
      register: _pdns_rotated_zone
      changed_when: false

    - name: Assert that the zone was provisioned through the rotated API key
      ansible.builtin.assert:
        that:
          - _pdns_rotated_zone.json.name == _probe_zone
          - >-
            (_pdns_rotated_zone.json.rrsets | selectattr('type', 'equalto', 'SOA') | list)
            | length == 1
        fail_msg: >-
          provisioning did not reach the instance with the rotated key:
          {{ _pdns_rotated_zone.json | default('no response') }}

    - name: Read the metadata provisioned with the rotated API key
      ansible.builtin.uri:
        headers:
          X-Api-Key: "{{ _rotated_api_key }}"
        url: "{{ _api_url }}/zones/{{ _probe_zone }}/metadata/ALLOW-AXFR-FROM"
        method: GET
        status_code:
          - 200
      register: _pdns_rotated_metadata
      changed_when: false

    - name: Assert that the metadata was provisioned as declared
      ansible.builtin.assert:
        that:
          - _pdns_rotated_metadata.json.metadata == ['127.0.0.0/8']
        fail_msg: "{{ _pdns_rotated_metadata.json | default('no response') | to_json }}"

    ##
    # Leave the host as the converge left it, for the verifier.
    ##
    - name: Remove the probe zone
      ansible.builtin.uri:
        headers:
          X-Api-Key: "{{ _rotated_api_key }}"
        url: "{{ _api_url }}/zones/{{ _probe_zone }}"
        method: DELETE
        status_code:
          - 204
      changed_when: true

    - name: Remove the drop-in of the operator
      ansible.builtin.file:
        path: "{{ _dropin_dir }}/10-operator.conf"
        state: absent

    - name: Restore the configured overrides
      ansible.builtin.include_role:
        name: powerdns.pdns
      vars:
        pdns_flush_handlers: true
