---

- name: Create the directory for the SQLite databases
  file:
    name: "{{ '/'.join(item.split('/')[:-1]) }}"
    owner: "{{ pdns_user }}"
    group: "{{ pdns_group }}"
    state: directory
  with_items: "{{ pdns_sqlite_databases_locations }}"

- name: Initiate the SQLite databases on RedHat
  shell: "sqlite3 {{ item }} < /usr/share/doc/pdns-backend-sqlite-{{ pdns_package_version | regex_replace('-[^-]*$', '') }}/schema.sqlite3.sql"
  args:
    creates: "{{ item }}"
  with_items: "{{ pdns_sqlite_databases_locations }}"
  when: ansible_os_family == "RedHat"

- name: Initiate the SQLite databases on Debian
  shell: "sqlite3 {{ item }} < /usr/share/doc/pdns-backend-sqlite3/schema.sqlite3.sql"
  args:
    creates: "{{ item }}"
  with_items: "{{ pdns_sqlite_databases_locations }}"
  when: ansible_os_family == "Debian"

- name: Check the SQLite databases permissions
  file:
    name: "{{ item }}"
    owner: "{{ pdns_user }}"
    group: "{{ pdns_group }}"
    state: file
  with_items: "{{ pdns_sqlite_databases_locations }}"
