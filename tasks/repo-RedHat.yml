---

- block:

  - name: Install epel-release on CentOS
    yum:
      name: epel-release
      state: present
    when: ansible_distribution in [ 'CentOS' ]

  - name: Install epel-release on RHEL/OracleLinux
    yum:
      name: "https://dl.fedoraproject.org/pub/epel/epel-release-latest-{{ ansible_distribution_major_version }}.noarch.rpm"
      state: present
    when: ansible_distribution in [ 'RedHat', 'OracleLinux' ]

  when: pdns_install_epel

- name: Install yum-plugin-priorities
  package:
    name: yum-plugin-priorities
    state: present
  when: ansible_distribution in [ 'CentOS' ]

- name: Add the PowerDNS Repository
  yum_repository:
    name: "{{ pdns_install_repo['name'] }}"
    description: PowerDNS Auth Repository
    baseurl: "{{ pdns_install_repo['yum_repo_baseurl'] }}"
    gpgkey: "{{ pdns_install_repo['gpg_key'] }}"
    gpgcheck: yes
    priority: 90
    state: present

- name: Add the PowerDNS debug Repository
  yum_repository:
    name: "{{ pdns_install_repo['name'] }}-debuginfo"
    description: PowerDNS Auth Repository
    baseurl: "{{ pdns_install_repo['yum_debug_repo_baseurl'] }}"
    gpgkey: "{{ pdns_install_repo['gpg_key'] }}"
    gpgcheck: yes
    priority: 90
    state: present
  when: pdns_install_debug_package
