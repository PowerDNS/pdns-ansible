[tox]
minversion = 1.8
envlist = ansible216
skipsdist = true

[gh-actions:env]
ANSIBLE=
  2.16: ansible216

[testenv]
passenv = *
deps =
    -rtest-requirements.txt
    ansible216: ansible-core>2.16,<2.17
setenv =
  PY_COLORS = 1
commands =
    {posargs:molecule test --all --destroy always}
